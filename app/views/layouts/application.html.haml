<!DOCTYPE html>
%html
  %head
    %title Гипермаркет "Огромно"
    -#= stylesheet_link_tag "kickstart/kickstart.css", :media => "all"
    = stylesheet_link_tag "application", :media => "all"
    -#= stylesheet_link_tag 'http://fonts.googleapis.com/css?family=PT+Sans:400,400italic,700,700italic&subset=latin,cyrillic', :media => "all"
    -#= javascript_include_tag "kickstart/kickstart.js.bak"
    = javascript_include_tag "application"
    = yield(:head)
    -#= javascript_include_tag :defaults
    = csrf_meta_tags
    %style{:type => 'text/css'} * {box-sizing: border-box; -moz-box-sizing: border-box;-webkit-box-sizing: border-box}
  %body
    = render 'shared/header'
    #wrapper
      %div{style: 'position:absolute;bottom:0px;width:800px;'}
        -if (flash[:success])
          .notice.success
            %i.icon-ok.icon-large
            = flash[:success]
            %a.icon-remove{:href => '#close'}
        -if (notice)
          .notice.warning
            %i.icon-warning-sign.icon-large
            = notice
            %a.icon-remove{:href => '#close'}
        -if (alert)
          .notice.error
            %i.icon-remove-sign.icon-large
            = alert
            %a.icon-remove{:href => '#close'}
      #main-data
        = render 'shared/banners'
        = yield
      #right-sidebar
        - unless current_user.nil?
          %ul
            -#%li= link_to t('banner.my_banners'), banners_path
            -#%li= link_to t('record.my_records'), records_path
            %li= link_to t('user.my_distribution_records'), get_package_path
      .clear
    <script type='text/javascript'>
    var googletag = googletag || {};
    googletag.cmd = googletag.cmd || [];
    (function() {
    var gads = document.createElement('script');
    gads.async = true;
    gads.type = 'text/javascript';
    var useSSL = 'https:' == document.location.protocol;
    gads.src = (useSSL ? 'https:' : 'http:') +
    '//www.googletagservices.com/tag/js/gpt.js';
    var node = document.getElementsByTagName('script')[0];
    node.parentNode.insertBefore(gads, node);
    })();
    </script>

    <script type='text/javascript'>
    googletag.cmd.push(function() {
    googletag.defineSlot('/54884489/pod_chatom_728x90', [728, 90], 'div-gpt-ad-1371211430929-0').addService(googletag.pubads());
    googletag.pubads().enableSingleRequest();
    googletag.enableServices();
    });
    </script>

    <!-- pod_chatom_728x90 -->
    <div id='div-gpt-ad-1371211430929-0' style='width:728px; height:90px;'>
    <script type='text/javascript'>
    googletag.cmd.push(function() { googletag.display('div-gpt-ad-1371211430929-0'); });
    </script>
    </div>
    = render 'shared/footer'
