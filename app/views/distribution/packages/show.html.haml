- package_list = @distribution_package.package_list
- point = package_list.point
%h4 Запись №#{ Russian::strftime package_list.date, '%d%m%Y'}/#{@distribution_package.code} на #{ Russian::strftime package_list.date, '%e %B'} в ЦР на #{point.address.to_s}
%h5
  %strong Статус записи: #{t('distribution.package.states.' + @distribution_package.state)}
%h6  Время работы Центра раздач:
%p
  = raw point.work_schedule
%p
  %strong Закупки к выдаче:
%table.tight
  %thead
    %tr
      %td Название закупки
      %td Организатор
  %tbody
    - @distribution_package.items.current_pickup.each do |item|
      %tr
        %td= t item.title
        %td= item.organizer
