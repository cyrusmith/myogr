%h4=point.title
%ul.tabs.left
  - if active == 'main'
    %li.current= link_to 'Главная', 'javascript:void(0);'
  -else
    %li= link_to 'Главная', distribution_point_path(point)

  - if active == 'package_lists'
    %li.current= link_to 'Ведомости', 'javascript:void(0);'
  -else
    %li= link_to 'Ведомости', distribution_point_package_lists_path(point)

  - if active == 'barcode_print'
    %li.current= link_to 'Печать штрих-кодов', 'javascript:void(0);'
  -else
    %li= link_to 'Печать штрих-кодов', new_distribution_barcode_path

  - if active == 'reception'
    %li.current= link_to 'Приход', 'javascript:void(0);'
  - else
    %li= link_to 'Приход', distribution_point_reception_path(point)

  - if active == 'issue_package'
    %li.current= link_to 'Выдача заказа', distribution_point_issue_package_path(point)
  -else
    %li= link_to 'Выдача заказа', distribution_point_issue_package_path(point)

  - if active == 'revision'
    %li.current= link_to 'Инвентаризация', 'javascript:void(0);'
  - else
    %li= link_to 'Инвентаризация', distribution_point_revision_path(point)