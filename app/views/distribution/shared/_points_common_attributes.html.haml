- if point.errors.any?
  #error_explanation
    %h2= "#{pluralize(point.errors.count, "error")} prohibited this distribution_center from being saved:"
    %ul
      - point.errors.full_messages.each do |msg|
        %li= msg
.field
  = form.label :title, t('.title')
  = form.text_field :title

.field
  = form.label :head_name, t('.head_user')
  = form.text_field :head_name, data: {autocomplete_source: find_users_by_name_path}

.field
  = form.label :employees_names, t('.employees')
  = form.text_field :employees_names, data: {autocomplete_source: find_users_by_name_path}

.field
  = form.label :default_day_package_limit, t('.default_package_limit')
  = form.text_field :default_day_package_limit, size: 1